# Runtime dependencies only — used by api/ app/ src/
# Training (optuna) and scraping (playwright, bs4, lxml) are excluded

# Data
pandas==2.2.3
numpy==2.0.2

# ML (CPU-only, no CUDA)
scikit-learn==1.5.2
xgboost-cpu==2.1.3
lightgbm==4.5.0
shap==0.46.0

# Experiment Tracking
mlflow==2.19.0

# API
fastapi==0.115.6
uvicorn==0.32.1
python-multipart==0.0.20

# Frontend
streamlit==1.41.1
requests<3,>=2.27

# Utils
pydantic==2.10.3
python-dotenv==1.0.1
matplotlib==3.9.4
joblib==1.4.2
